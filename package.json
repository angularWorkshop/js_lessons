{
  "name": "@angular-workshop/js-lessons",
  "version": "0.1.0",
  "private": true,
  "description": "Base repository for EduTec JavaScript course lessons",
  "stackblitz": {
    "startCommand": "npm run start:dev"
  },
  "type": "module",
  "scripts": {
    "start:dev": "node scripts/stackblitz-status.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
