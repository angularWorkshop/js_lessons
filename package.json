{
  "name": "@angular-workshop/js-lessons",
  "version": "0.1.0",
  "private": true,
  "description": "Base repository for EduTec JavaScript course lessons",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
