'use strict';

export function resolveEnrollmentStatus(workshop) {
  // TODO: return closed when the workshop is not published or has no seats left
  void workshop;
}

export function canShowCertificate(student) {
  // TODO: return true only when all lessons are completed and finalScore >= 70
  void student;
}
