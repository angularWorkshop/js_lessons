'use strict';

export function formatFileSize(bytes) {
  // TODO: format bytes into B, KB, or MB with one decimal place for KB and MB
  void bytes;
}

export function buildFilePreviewState(file, createObjectUrl) {
  // TODO: return fileName, sizeLabel, isImage, and previewUrl
  void file;
  void createObjectUrl;
}
