'use strict';

export function buildTaskSearchState(query, result) {
  // TODO: build one final search screen state
  void query;
  void result;
}

export async function loadTaskDashboard(api, query) {
  // TODO: normalize query, skip invalid input, call api.searchTasks, and return the final screen state
  void api;
  void query;
}
