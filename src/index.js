'use strict';

export function normalizeVisitorName(name) {
  // TODO: trim the input and return Guest when the result is empty
  void name;
}

export function parseParticipants(rawValue) {
  // TODO: convert to a positive integer, fallback to 1 when invalid
  void rawValue;
}

export function buildSeatLabels(count) {
  // TODO: return labels Seat 1, Seat 2, ... up to count
  void count;
}

export function buildPlaygroundState(input) {
  // TODO: use helpers, apply a 20% promo discount when hasPromo is true and participants >= 3,
  // and return visitorName, participants, seatLabels, total, and accessLabel
  void input;
}
