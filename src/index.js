'use strict';

export function escapeHtml(value) {
  // TODO: escape the dangerous HTML characters
  void value;
}

export function renderCommentCard(author, comment) {
  // TODO: build the HTML with the escaped comment text
  void author;
  void comment;
}
