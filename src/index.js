'use strict';

export function escapeHtml(value) {
  // TODO: escape &, <, >, ", and '
  void value;
}

export function applyIncomingComment(state, event, allowedOrigin) {
  // TODO: validate origin, type, and payload shape, then append a safe comment or increment rejectedCount
  void state;
  void event;
  void allowedOrigin;
}
